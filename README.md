#Europa Cycloid Modeling

## Launch Instructions
* Install Docker
* Run `docker-compose build`
* Run `docker-compose up -d`
* Launch web browser and navigate to `http://localhost:8899`

Notebook password is `rhinos`


## Shutdown Server
* Run `docker-compose down`